<main data-controller="user"class="user">
    <section class="content" >
        <div class="container">
            {% if page.help %}
                <div class="row">
                    <div class="col-12 col-md-8 col-lg-6 offset-md-2 offset-lg-3">
                        <div class="alert alert-success mb-4" role="alert">
                          {{ page.help }}
                        </div>
                    </div>
                </div>
            {% endif %}
            <div class="row">
                <div class="col-12 col-md-8 col-lg-6 offset-md-2 offset-lg-3">
                    {% if logged_in %}
                        <ul class="userNav">
                            <li>
                                <a href="{{userMenu.editar}}">MEUS DADOS</a>
                            </li>
                            <li class="active">
                                <a href="/user/oficinas">OFICINAS</a>
                            </li>
                        </ul>
                    {% endif %}
                    <form class="formOficinas" method="POST" action="/user/oficinas">
                        <div class="alert alert-success d-none mb-4" role="alert">
                            Incrição realizada com sucesso, acompanhe seu email para saber os próximos passos. 
                        </div>
                        <p class="mb-4"><strong>Selecione abaixo as oficinas e/ou atividades desejadas:</strong></p>
                        
                         {% for key, oficina in customPage.conteudo.oficinas.itens %}
                            <div class="form-check">
                                {% if not oficina.filhos %}
                                    <input class="form-check-input" type="checkbox" value="{{oficina.nid}}" {{(oficina.nid in customPage.minhasInscricoes) ? 'checked  disabled="disabled" ' : ''}} name="Oficinas[]" id="oficina{{key}}">
                                    <label class="form-check-label" for="oficina{{key}}">
                                        <p>
                                            {{oficina.tipo}}: {{oficina.titulo}}<br/>
                                            <strong>Valor: </strong>{{oficina.investimento}}<br/>
                                        </p>
                                    </label>
                                    <div class="detalheOfinica">
                                        <a href="javascript:void(0);"class="btn btn-sm btn-dark"><span>+</span> detalhes</a>
                                        <div class="texto">
                                            <p>{{oficina.descricao}}</p><br>
                                            <p>
                                                {% if oficina.data %}Data da oficina: {{oficina.data}} - {{oficina.horario}}<br>{% endif %}
                                                {% if oficina.facilitacao %}
                                                Facilitação: {{oficina.facilitacao}}<br>
                                                {% endif %}
                                                Duração: {{oficina.duracao}}
                                            </p>
                                        </div>
                                    </div>
                                {% else %}
                                    <p>
                                        {{oficina.tipo}}: {{oficina.titulo}}<br/>
                                        <strong>Valor: </strong>{{oficina.investimento}}<br/>
                                    </p>
                                    <div class="detalheOfinica">
                                        <a href="javascript:void(0);"class="btn btn-sm btn-dark"><span>+</span> detalhes</a>
                                        <div class="texto">
                                            <p>{{oficina.descricao}}</p><br>
                                            <p>
                                                {#Data da oficina: {{oficina.data}}<br>#}
                                                Duração: {{oficina.duracao}}
                                            </p>
                                        </div>
                                        
                                    </div>
                                    <div style="padding: 0 0 26px 25px;">
                                    {% for keyOf, filhoOf in oficina.filhos %}
                                            <div>
                                                <input class="form-check-input" type="checkbox" name="Oficinas[]" value="{{filhoOf.nid}}" {{(filhoOf.nid in customPage.minhasInscricoes) ? 'checked  disabled="disabled" ' : ''}} id="oficinaOf{{keyOf}}">
                                                <label class="form-check-label" for="oficinaOf{{keyOf}}">
                                                    <p>
                                                        <span style="display:block; position:absolute; width:1px; height:px; opacity:0">{{filhoOf.tipo}}: {{filhoOf.titulo}} - </span>{{filhoOf.horario}}
                                                    </p>
                                                </label>
                                            </div>
                                        {% endfor %}
                                        </div>
                                {% endif %}
                            </div>
                        {% endfor %}
                        
                        
                        {#DADOS ADICIONAIS#}
                        <p class="mb-4 mt-5"><strong>Informações adicionais para as oficinas:</strong></p>
                        <div class="form-group">
                            <input class="form-control" required id="nascimento" name="Nascimento" type="text" placeholder="Data de nascimento" value="{{customPage.infoForm.field_nascimento.0.value}}">
                        </div>
                        <div class="form-group">
                            <input class="form-control" required id="telefone" name="Telefone" type="text" placeholder="Telefone" value="{{customPage.infoForm.field_telefone.0.value}}">
                        </div>
                        <div class="form-group">
                            <input class="form-control" required id="cep" name="CEP" type="text" placeholder="CEP" value="{{customPage.infoForm.field_cep.0.value}}">
                            <div class="invalid-feedback">
                              Digite um CEP válido.
                            </div>
                        </div>
                        <div class="form-group">
                            <input class="form-control" required id="endereco" name="Endereco" type="text" placeholder="Endereço completo" value="{{customPage.infoForm.field_endereco.0.value}}">
                        </div>
                        <div class="form-group">
                            <input class="form-control" required id="numero" name="Numero" type="text" placeholder="Número" value="{{customPage.infoForm.field_numero.0.value}}">
                        </div>
                        <div class="form-group">
                            <input class="form-control" id="complemento" name="Complemento" type="text" placeholder="Complemento" value="{{customPage.infoForm.field_complemento.0.value}}">
                        </div>
                    
                        <button type="submit" class="btnSubmit btn btn-lg btn-success w-100 mt-4">Finalizar Inscrição</button>
                    </form>
                </div>
            </div>
        </div>
        <div class="backupParallax" style="background-image: url(/themes/futurospossiveis2021/static/img/bg_topo_home.jpg);"></div>
        <div class="parallax" data-parallax-image="/themes/futurospossiveis2021/static/img/bg_topo_home.jpg"></div>
    </section>
</main>